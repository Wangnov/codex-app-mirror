{
  "name": "codex-app-mirror-download-router",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --test test/*.mjs && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --test test/*.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
